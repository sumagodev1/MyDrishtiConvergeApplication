package com.mydrishti.co.`in`.activities.tutorial

/**
 * Enum defining tutorial completion status
 */
enum class CompletionStatus {
    NOT_STARTED,
    IN_PROGRESS,
    COMPLETED,
    SKIPPED,
    FAILED
}