package com.mydrishti.co.`in`.activities.tutorial

/**
 * Enum defining different animation types for tutorial steps
 */
enum class AnimationType {
    FADE,
    SCALE,
    SLIDE_IN,
    PULSE,
    RIPPLE,
    CELEBRATION,
    FADE_SCALE,
    NONE
}